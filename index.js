"use strict";
// Placeholder for WS DNS server (ad-hoc discovery)
// Implement REST endpoints for /v1/set/:service , /v1/get/:service , /v1/ping

module.exports = function(setup) {
  // TODO: implement DNS registry server
  return { start: () => console.log('ws-dns placeholder') };
};
